// Criar uma função que recebe um número como parâmetro e retorna o resultado da multiplicação desse número por ele mesmo

function multiplicandoPorEleMesmo(numero){
    let multiplicado = numero * numero;
    console.log(`O Número ${numero} multiplicado por ele mesmo é ${multiplicado}.`);
}

multiplicandoPorEleMesmo(10);